% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{main_iterations}
\alias{main_iterations}
\title{Main iterative updates for BiSSLB model using RcppArmadillo.}
\usage{
main_iterations(
  mu,
  A,
  B,
  tilde_thetas,
  thetas,
  Y,
  xi,
  eta,
  tilde_lambdas,
  lambdas,
  tilde_alpha,
  tilde_beta,
  alpha,
  beta,
  tol,
  max_iter,
  IBP,
  I,
  J
)
}
\arguments{
\item{mu}{A numeric vector representing the baseline signal (location parameter).}

\item{A}{A numeric matrix representing initial row profiles.}

\item{B}{A numeric matrix representing initial column profiles.}

\item{tilde_thetas}{A numeric vector of initial sparsity parameters for \eqn{A}.}

\item{thetas}{A numeric vector of initial sparsity parameters for \eqn{B}.}

\item{Y}{A binary observation matrix to be approximated.}

\item{xi}{A positive scalar controlling the pseudo-likelihood scale.}

\item{eta}{A small positive scalar controlling soft-thresholding aggressiveness.}

\item{tilde_lambdas}{Spike-and-slab prior lambdas for \eqn{A}.}

\item{lambdas}{Spike-and-slab prior lambdas for \eqn{B}.}

\item{tilde_alpha}{Shape parameter of Beta prior on \eqn{\tilde{\theta}} (for A).}

\item{tilde_beta}{Rate parameter of Beta prior on \eqn{\tilde{\theta}} (for A).}

\item{alpha}{Shape parameter of Beta prior on \eqn{\theta} (for B).}

\item{beta}{Rate parameter of Beta prior on \eqn{\theta} (for B).}

\item{tol}{Convergence tolerance for relative change in log-likelihood.}

\item{max_iter}{Maximum number of EM-like iterations.}

\item{IBP}{Integer flag (1 = enable, 0 = disable) for IBP-style column reordering.}

\item{I}{Number of rows in \eqn{A} (features).}

\item{J}{Number of rows in \eqn{B} (samples).}
}
\value{
A list containing:
\describe{
  \item{A}{Final row profile matrix \eqn{A}.}
  \item{B}{Final column profile matrix \eqn{B}.}
  \item{mu}{Updated location parameter vector.}
  \item{tilde_thetas}{Updated sparsity vector for \eqn{A}.}
  \item{thetas}{Updated sparsity vector for \eqn{B}.}
  \item{delta_logLikelihood_save}{Vector of log-likelihood changes per iteration.}
  \item{logLikelihood_save}{Vector of log-likelihood values per iteration.}
  \item{BIC}{Final Bayesian Information Criterion (BIC) value.}
}
}
\description{
This function performs the core iterative updates for the Binary Spike-and-Slab
Lasso Biclustering (BiSSLB) model. It alternates between updating the latent
row profile matrix \eqn{A}, column profile matrix \eqn{B}, and sparsity parameters
(\eqn{\theta}, \eqn{\tilde{\theta}}) under spike-and-slab priors. It supports
optional Indian Buffet Process (IBP) reordering and soft-thresholding.
}
\keyword{internal}
