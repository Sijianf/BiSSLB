% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{BiSSLB}
\alias{BiSSLB}
\title{Bi-cluster a binary matrix using Binary Spike-and-Slab Lasso.}
\usage{
BiSSLB(
  Y,
  A = NULL,
  B = NULL,
  xi = 1,
  mu = NULL,
  tilde_lambda_0 = 5,
  tilde_lambda_1 = 1,
  lambda_0 = 5,
  lambda_1 = 1,
  method = "svd",
  tilde_alpha = 1,
  tilde_beta = 1,
  alpha = 1,
  beta = 1,
  K_init = 50,
  thisSeed = 123,
  eta = 0.001,
  IBP = 1,
  tol = 1e-10,
  max_iter = 500,
  show_plot = FALSE
)
}
\arguments{
\item{Y}{A binary matrix to be bi-clustered (rows = features, columns = samples).}

\item{A}{Optional initialization for the row profile matrix \eqn{A}.}

\item{B}{Optional initialization for the column profile matrix \eqn{B}.}

\item{xi}{A positive value representing confidence dispersion (deprecated).}

\item{mu}{Optional initialization of the baseline signal (location parameter).}

\item{tilde_lambda_0}{The "spike" (strong shrinkage) parameter for the prior on matrix \eqn{A}.}

\item{tilde_lambda_1}{The "slab" (weak shrinkage) parameter for the prior on matrix \eqn{A}.}

\item{lambda_0}{The "spike" (strong shrinkage) parameter for the prior on matrix \eqn{B}.}

\item{lambda_1}{The "slab" (weak shrinkage) parameter for the prior on matrix \eqn{B}.}

\item{method}{A character string indicating how to initialize latent matrices; default is "svd".}

\item{tilde_alpha}{Shape parameter for the Beta prior on sparsity of \eqn{A}.}

\item{tilde_beta}{Rate parameter for the Beta prior on sparsity of \eqn{A}.}

\item{alpha}{Shape parameter for the Beta prior on sparsity of \eqn{B}.}

\item{beta}{Rate parameter for the Beta prior on sparsity of \eqn{B}.}

\item{K_init}{Initial number of latent factors (columns of \eqn{A}, \eqn{B}).}

\item{thisSeed}{An integer seed for reproducibility.}

\item{eta}{A small positive value controlling soft-thresholding smoothness.}

\item{IBP}{Logical flag (1 = TRUE, 0 = FALSE) to enable IBP-style feature selection and reordering.}

\item{tol}{Convergence tolerance for log-likelihood change.}

\item{max_iter}{Maximum number of EM iterations.}

\item{show_plot}{Logical; whether to plot convergence diagnostics.}
}
\value{
A list containing estimated matrices \eqn{A}, \eqn{B}, and other inference outputs.
}
\description{
This function performs bi-clustering on a binary matrix using a Bayesian
spike-and-slab Lasso prior (BiSSLB). The binary matrix is approximated by
the product of two latent matrices \eqn{A} and \eqn{B}, representing row-
and column-specific profiles, respectively. The method uses spike-and-slab
priors to enforce sparsity in the latent structures and iteratively estimates
the underlying patterns. This function acts as the R interface for the main
C++ engine and includes post-processing such as reordering and thresholding.
}
\examples{
Y <- matrix(rbinom(100, 1, 0.5), 10, 10)
out <- BiSSLB(Y, K_init = 5)

}
