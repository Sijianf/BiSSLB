% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{BiSSLB_ladder}
\alias{BiSSLB_ladder}
\title{Run BiSSLB across a ladder of spike parameters for model selection.}
\usage{
BiSSLB_ladder(
  Y,
  xi = 1,
  A = NULL,
  B = NULL,
  mu = NULL,
  tilde_lambda_0s,
  tilde_lambda_1,
  lambda_0s,
  lambda_1,
  method = "svd",
  tilde_alpha = 1,
  tilde_beta = 1,
  alpha = 1,
  beta = 1,
  K_init = 30,
  thisSeed = 123,
  eta = 0.001,
  IBP = 1,
  tol = 1e-10,
  max_iter = 1000,
  show_plot = FALSE
)
}
\arguments{
\item{Y}{A binary matrix to be bi-clustered (rows = features, columns = samples).}

\item{xi}{A positive value representing confidence dispersion (deprecated).}

\item{A}{Optional initialization for the row profile matrix \eqn{A}.}

\item{B}{Optional initialization for the column profile matrix \eqn{B}.}

\item{mu}{Optional initialization of the baseline signal (location parameter).}

\item{tilde_lambda_0s}{A numeric vector of spike values for matrix \eqn{A}.}

\item{tilde_lambda_1}{The shared slab value for matrix \eqn{A}.}

\item{lambda_0s}{A numeric vector of spike values for matrix \eqn{B}.}

\item{lambda_1}{The shared slab value for matrix \eqn{B}.}

\item{method}{A character string indicating how to initialize latent matrices; default is "svd".}

\item{tilde_alpha}{Shape parameter for the Beta prior on sparsity of \eqn{A}.}

\item{tilde_beta}{Rate parameter for the Beta prior on sparsity of \eqn{A}.}

\item{alpha}{Shape parameter for the Beta prior on sparsity of \eqn{B}.}

\item{beta}{Rate parameter for the Beta prior on sparsity of \eqn{B}.}

\item{K_init}{Initial number of latent factors (columns of \eqn{A}, \eqn{B}).}

\item{thisSeed}{An integer seed for reproducibility.}

\item{eta}{A small positive value controlling soft-thresholding smoothness.}

\item{IBP}{Logical flag (1 = TRUE, 0 = FALSE) to enable IBP-style feature selection and reordering.}

\item{tol}{Convergence tolerance for log-likelihood change.}

\item{max_iter}{Maximum number of EM iterations.}

\item{show_plot}{Logical; whether to plot convergence or BIC across ladders.}
}
\value{
A list containing the results of each BiSSLB run, typically including:
\describe{
  \item{results}{A list of BiSSLB outputs for each (\code{tilde_lambda_0}, \code{lambda_0}) pair.}
  \item{BICs}{A matrix or data frame of corresponding BIC values.}
}
}
\description{
This function calls \code{\link{BiSSLB}} multiple times using different values
of \code{tilde_lambda_0} and \code{lambda_0}, forming a "ladder" of spike
parameters. This allows users to evaluate the stability and sparsity of the
biclustering solution across a range of regularization strengths, and select
the best combination based on performance criteria such as BIC.
}
\examples{
Y <- matrix(rbinom(100, 1, 0.5), 10, 10)
out <- BiSSLB_ladder(Y, tilde_lambda_0s = c(5, 10), lambda_0s = c(5, 10),
                     tilde_lambda_1 = 1, lambda_1 = 1)

}
